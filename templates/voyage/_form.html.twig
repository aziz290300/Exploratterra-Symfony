{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
{{ form_errors(form) }}
    <button type="submit" class="btn btn-primary">{{ button_label|default('Enregister') }}</button>

{{ form_end(form) }}
