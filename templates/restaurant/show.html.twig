{% extends 'base-back.html.twig' %}

{% block title %}Restaurant{% endblock %}

{% block body %}
    <h1>Restaurant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant.id }}</td>
            </tr>
            <tr>
                <th>NameRestaurant</th>
                <td>{{ restaurant.nameRestaurant }}</td>
            </tr>
            <tr>
                <th>RatingRestaurant</th>
                <td>{{ restaurant.ratingRestaurant }}</td>
            </tr>
            <tr>
                <th>ImageRestaurant</th>
                <td>{{ restaurant.imageRestaurant }}</td>
            </tr>
            <tr>
                <th>DescriptionRestaurant</th>
                <td>{{ restaurant.descriptionRestaurant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_restaurant_index') }}">back to list</a>

    <a href="{{ path('app_restaurant_edit', {'id': restaurant.id}) }}">edit</a>

    {{ include('restaurant/_delete_form.html.twig') }}
{% endblock %}
