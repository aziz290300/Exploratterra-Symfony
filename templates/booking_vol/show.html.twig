{% extends 'base-back.html.twig' %}

{% block title %}BookingVol{% endblock %}

{% block body %}
    <h1>BookingVol</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ booking_vol.id }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ booking_vol.userId }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ booking_vol.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_booking_vol_index') }}">back to list</a>

    <a href="{{ path('app_booking_vol_edit', {'id': booking_vol.id}) }}">edit</a>

    {{ include('booking_vol/_delete_form.html.twig') }}
{% endblock %}
