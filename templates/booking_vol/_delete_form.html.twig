<form method="post" action="{{ path('app_booking_vol_delete', {'id': booking_vol.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booking_vol.id) }}">
    <button class="btn">Delete</button>
</form>
