{% extends 'base-back.html.twig' %}

{% block title %}BookingVol index{% endblock %}

{% block body %}
    <h1>BookingVol index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>User_id</th>
                <th>Nombre</th>
               
            </tr>
        </thead>
        <tbody>
        {% for booking_vol in booking_vols %}
            <tr>
                <td>{{ booking_vol.id }}</td>
                <td>{{ booking_vol.userId }}</td>
                <td>{{ booking_vol.nombre }}</td>
                <td>
                 
                  
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   
{% endblock %}
